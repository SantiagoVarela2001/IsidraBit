[build]
  command = "npm run build"  # Ajusta esto según tu configuración
  publish = "dist"  # Ajusta esto según tu configuración de salida

  [[redirects]]
  from = "/IsidraBit - Back/*"
  to = "http://localhost:8080/:splat"
  status = 200
  force = true